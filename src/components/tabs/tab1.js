import React from 'react';
import { Button, Text } from 'native-base';

const Tab1 = () => {
    return (
            <Button>
            <Text>Click Me! </Text>
            </Button>
    );
};

export default Tab1;
